<dx:ThemedWindow x:Class="ZeroPlus.Oms.Ui.Views.RemoveByWidthPromptView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                 xmlns:sys="clr-namespace:System;assembly=mscorlib"
                 Title="Clear By Width"
                 Width="290"
                 Height="170"
                 Topmost="True"
                 WindowStartupLocation="CenterScreen"
                 WindowStyle="ToolWindow">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:CurrentWindowService />
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <Label Grid.Row="1"
               Grid.Column="1"
               Margin="5,5,0,0"
               Content="Min" />

        <Label Grid.Row="1"
               Grid.Column="2"
               Margin="5,5,0,0"
               Content="Max" />

        <dxe:SpinEdit Grid.Row="2"
                      Grid.Column="1"
                      Margin="5,0,0,0"
                      AllowSpinOnMouseWheel="True"
                      EditValue="{Binding MinWidthForClear, UpdateSourceTrigger=PropertyChanged}"
                      EditValueType="{x:Type sys:Double}"
                      Increment=".01"
                      Mask="N2"
                      MinValue="0"
                      PreviewMouseUp="PriceSpinEdit_MouseUp"
                      SelectAllOnGotFocus="True"
                      SelectAllOnMouseUp="True" />

        <dxe:SpinEdit Grid.Row="2"
                      Grid.Column="2"
                      Margin="5,0,0,0"
                      AllowSpinOnMouseWheel="True"
                      EditValue="{Binding MaxWidthForClear, UpdateSourceTrigger=PropertyChanged}"
                      EditValueType="{x:Type sys:Double}"
                      Increment=".01"
                      Mask="N2"
                      MinValue="0"
                      PreviewMouseUp="PriceSpinEdit_MouseUp"
                      SelectAllOnGotFocus="True"
                      SelectAllOnMouseUp="True" />

        <StackPanel Grid.Row="4"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Margin="0,5,0,0"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <dx:SimpleButton Width="70"
                             Margin="5,0"
                             Click="CloseClick"
                             Command="{Binding ClearAllOutsideWidthCommand}"
                             Content="Clear"
                             IsDefault="True" />

            <dx:SimpleButton Width="70"
                             Margin="5,0"
                             Click="CloseClick"
                             Content="Cancel"
                             IsCancel="True" />
        </StackPanel>
    </Grid>
</dx:ThemedWindow>
