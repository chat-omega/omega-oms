<dx:ThemedWindow x:Class="ZeroPlus.Oms.Ui.Views.ModifyStagedOrdersView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                 Title="Modify Staged Orders"
                 Width="290"
                 Height="170"
                 Topmost="True"
                 WindowStartupLocation="CenterScreen"
                 WindowStyle="ToolWindow">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:CurrentWindowService />
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <Label Grid.Row="1"
               Grid.Column="1"
               Margin="0,5,0,0"
               Content="Account:" />

        <dxe:ComboBoxEdit Grid.Row="1"
                          Grid.Column="2"
                          Margin="5,5,0,0"
                          Background="#171718"
                          IsTextEditable="False"
                          ItemsSource="{Binding AccountsList}"
                          SelectedItem="{Binding Account}" />

        <Label Grid.Row="2"
               Grid.Column="1"
               Margin="0,5,0,0"
               Content="TIF:" />

        <dxe:ComboBoxEdit Grid.Row="2"
                          Grid.Column="2"
                          Margin="5,5,0,0"
                          Background="#171718"
                          IsTextEditable="False"
                          ItemsSource="{Binding TifsList}"
                          SelectedItem="{Binding Tif}" />

        <Label Grid.Row="3"
               Grid.Column="1"
               Margin="0,5,0,0"
               Content="Route:" />

        <dxe:ComboBoxEdit Grid.Row="3"
                          Grid.Column="2"
                          Margin="5,5,0,0"
                          Background="#171718"
                          IsTextEditable="True"
                          ItemsSource="{Binding RoutesList}"
                          SelectedItem="{Binding Route}" />

        <StackPanel Grid.Row="5"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Margin="0,5,0,0"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <dx:SimpleButton Width="150"
                             Margin="5,0"
                             Command="{Binding ModifyCommand}"
                             Content="Modify Staged Orders"
                             IsDefault="True" />
            <dx:SimpleButton Width="70"
                             Margin="5,0"
                             Command="{Binding CancelCommand}"
                             Content="Cancel"
                             IsCancel="True" />
        </StackPanel>

    </Grid>
</dx:ThemedWindow>
