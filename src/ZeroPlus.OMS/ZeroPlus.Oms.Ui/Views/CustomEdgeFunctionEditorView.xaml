<dx:ThemedWindow x:Class="ZeroPlus.Oms.Ui.Views.CustomEdgeFunctionEditorView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:dexp="http://schemas.devexpress.com/winfx/2008/xaml/expressioneditor"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:startupHelpers="clr-namespace:ZeroPlus.Oms.Ui.StartupHelpers"
                 xmlns:viewModels="clr-namespace:ZeroPlus.Oms.Ui.ViewModels"
                 Title="{Binding ModuleTitle}"
                 Width="1000"
                 Height="500"
                 MinWidth="500"
                 MinHeight="300"
                 d:DataContext="{d:DesignInstance Type=viewModels:CustomEdgeFunctionEditorViewModel}"
                 DataContext="{startupHelpers:DISource Type=viewModels:CustomEdgeFunctionEditorViewModel}"
                 Topmost="True"
                 WindowStartupLocation="CenterScreen"
                 WindowStyle="ToolWindow"
                 mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <dexp:ExpressionEditorControl Name="editor"
                                      Grid.Row="0"
                                      EditValue="{Binding Formula}" />
        <dx:SimpleButton Grid.Row="1"
                         Width="80"
                         Margin="2"
                         HorizontalAlignment="Center"
                         Click="OnSave"
                         Content="Save" />
    </Grid>
</dx:ThemedWindow>
