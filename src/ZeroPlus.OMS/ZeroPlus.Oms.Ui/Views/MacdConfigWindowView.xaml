<dx:ThemedWindow x:Class="ZeroPlus.Oms.Ui.Views.MacdConfigWindowView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:startuphelpers="clr-namespace:ZeroPlus.Oms.Ui.StartupHelpers"
                 xmlns:viewmodels="clr-namespace:ZeroPlus.Oms.Ui.ViewModels"
                 xmlns:views="clr-namespace:ZeroPlus.Oms.Ui.Views"
                 Title="Ema Configuration"
                 Width="300"
                 Height="350"
                 MinWidth="250"
                 MinHeight="300"
                 d:DataContext="{d:DesignInstance Type=viewmodels:MacdConfigWindowViewModel}"
                 DataContext="{startuphelpers:DISource Type=viewmodels:MacdConfigWindowViewModel}"
                 Topmost="True"
                 WindowStartupLocation="CenterScreen"
                 WindowStyle="ToolWindow"
                 mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <dxlc:GroupBox Grid.Row="1"
                       Grid.Column="1"
                       Margin="0,2,0,0"
                       Header="Slow EMA Config">
            <views:EmaConfigView DataContext="{Binding SlowEmaConfigViewModel}" />
        </dxlc:GroupBox>
        <dxlc:GroupBox Grid.Row="2"
                       Grid.Column="1"
                       Margin="0,2,0,0"
                       Header="Fast EMA Config">
            <views:EmaConfigView DataContext="{Binding FastEmaConfigViewModel}" />
        </dxlc:GroupBox>
        <dxlc:GroupBox Grid.Row="3"
                       Grid.Column="1"
                       Margin="0,2,0,0"
                       Header="Signal EMA Config">
            <views:EmaConfigView DataContext="{Binding SignalEmaConfigViewModel}" />
        </dxlc:GroupBox>
    </Grid>
</dx:ThemedWindow>
