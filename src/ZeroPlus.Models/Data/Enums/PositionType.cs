namespace ZeroPlus.Models.Data.Enums
{
    public enum PositionType : byte
    {
        Main,
        Spread,
        Underlying,
        Instance,
        BaseStrategy,
        Symbol,
        Route,
        Exchange,
        Expiration,
    }
}
